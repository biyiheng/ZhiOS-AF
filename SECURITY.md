# 安全策略（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| v1.1.x（release 分支） | ✅ 安全更新支持中 |
| main（开发分支） | ⚠️ 不保证向后兼容，仅供开发 |

## 报告漏洞

ZhiOS-AF 面向嵌入式实时操作系统场景，安全问题可能影响设备安全与人身安全。
若您发现安全漏洞，请**不要**在公开 issue 中披露，而应通过私密渠道报告：

- 邮箱 / 私信维护者（见仓库信息）。
- 或向 GitHub 提交私密安全公告（Security Advisory）。

请附上：
- 漏洞类型与严重程度评估；
- 可复现步骤（含目标平台/版本）；
- 受影响的模块与建议修复方向（如适用）。

我们会尽快响应并给出处置时间表。

## 已知安全基线

项目在设计与实现层面执行的安全基线（详见《33-操作系统技术指标体系设计文档》与
`docs/agent控制软件技术文档.md` 安全章节）：
- 禁止缓冲区溢出、整数回绕、空指针解引用、TOCTOU 竞态；
- 通信防火墙默认拒绝 + 白名单 + 令牌桶限速 + 载荷长度上限（413/名称/JSON 校验）；
- 敏感凭据经环境变量注入，不落盘、不硬编码；
- 硬实时场景建议启用 MPU 栈守护、Canary 越界检测与看门狗复位。

## 披露流程
1. 收到报告后 3 个工作日内确认并评估。
2. 修复并回归（`make test` + `zhio_sim.py` + `e2e_test.py` 全通过）。
3. 发布补丁并更新 RELEASE_NOTES 与 DEVELOPMENT_LOG。
